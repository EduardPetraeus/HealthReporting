Start a new feature branch for: $ARGUMENTS

1. Run `git checkout main && git pull origin main`
2. Create branch: `git checkout -b feature/$ARGUMENTS`
3. Confirm the branch is active and working tree is clean
