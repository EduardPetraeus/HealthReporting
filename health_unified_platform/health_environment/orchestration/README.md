# Orchestration

Job scheduling and pipeline trigger definitions.

Planned content:
- Databricks workflow YAML definitions (bronze → silver → gold chain)
- Retry and alerting configuration
- Dependency graphs between pipeline stages

Note: Current Databricks job definitions live in `databricks_framework/workflows/`.
