# Quality Assurance

Data quality checks and validation rules.

Planned content:
- dbt tests (not-null, unique, accepted-values per entity)
- Row count reconciliation between bronze and silver
- Freshness checks (last ingested timestamp per source)
