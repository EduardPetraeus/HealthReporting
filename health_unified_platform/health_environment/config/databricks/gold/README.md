# Databricks Gold Configs

**Status: 1 gold entity configured** — work in progress.

One YAML file per gold entity. Each file specifies the entity type (view/table), target table name, and SQL file reference.

## Configured
- `daily_heart_rate_summary.yml`

## Remaining
Gold layer is minimal by design — add configs here as reporting needs grow.
