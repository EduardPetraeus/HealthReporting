# Contracts

Data contracts defining expected schema, types, and constraints for silver and gold tables.

Planned content:
- YAML contracts per entity (e.g., `heart_rate.yml`, `daily_sleep.yml`)
- Schema expectations (column names, types, nullability)
- Row-count and freshness SLAs
